#include <iostream>
#include <math.h>

using namespace std;

int main(){
	float edge;
	float volume = pow(edge , 3);
	cout << "Enter the length of the edge of the cube: ";
	cin >> edge;
	
	cout << "The volume is : " << edge;
	return 0;
}
